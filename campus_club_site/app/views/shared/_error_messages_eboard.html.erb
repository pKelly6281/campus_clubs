<% if @eboard.errors.any? %>
	<div id="error_explanation">
		<div class="alert alert-danger">
			The form contains <%= pluralize(@eboard.errors.count, "error") %>.
		</div>
		<ul>
			<% @eboard.errors.full_messages.each do |msg| %>
				<li><%= msg %> </li>
			<% end %>
		</ul>
	</div>
<% end %>
